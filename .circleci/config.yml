version: 2.1

# docker:
#   - image: circleci/android:api-29

orbs:
  android: circleci/android@0.2.0

jobs:
  build:
    executor: android/android

    steps:
      - checkout
      - run:
          command: ./gradlew build;
                    mkdir /apk/jackknife_kotlin_samples;
      - store_artifacts:
          path: release/app-release.apk
          destination: /apk/jackknife_kotlin_samples
